# Copy to .env, fill in. NEVER commit real values.
# Reference oracle (harness only — never shipped in src/):
MILTON_REFERENCE_GGUF=/path/to/nomic-embed-text-v1.5.Q8_0.gguf
MILTON_REFERENCE_LLAMACPP=/path/to/llama.cpp
# Gate tolerance (derived from reference self-determinism — see HARNESS-SPEC.md):
MILTON_EPSILON=
MILTON_EPSILON_ABS=
