# Copy to .env, fill in. NEVER commit real values.
# Reference oracle (harness only — never shipped in src/).
# Defaults match Flair's current path: nomic-embed-text-v1.5.Q4_K_M.gguf
# (sha256 d4e388894e09cf3816e8b0896d81d265b55e7a9fff9ab03fe8bf4ef5e11295ac).
MILTON_REFERENCE_GGUF=/path/to/nomic-embed-text-v1.5.Q4_K_M.gguf
MILTON_REFERENCE_LLAMACPP=/path/to/llama.cpp
MILTON_REFERENCE_LLAMA_EMBEDDING=/path/to/llama-embedding
# Gate tolerance is committed in harness/goldens/epsilon.json (derived, not guessed).
# Override only for experiments — never loosen to pass.
MILTON_EPSILON=
MILTON_EPSILON_ABS=
