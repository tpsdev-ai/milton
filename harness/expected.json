{
  "schema": "milton.wasm-compare.expected/1",
  "expected": "pass",
  "reason": "native-Milton vs WASM-Milton are bit-identical: shared permissive exp (#26) and shared permissive sin/cos for RoPE (#27). Softmax / silu / V-mix / RoPE use the same mul+add kernels on both backends. Tight 1e-6/1e-5 compare is the lock (n=18 failed=0, max_abs=0).",
  "blocked_on": [],
  "note": "Two-way expected-outcome (flair#1468 shape). This lane is RED only when the observed wasm:compare result differs from `expected` in either direction — an unexpected FAIL is a regression. An unexpected PASS is red if this file still says fail."
}
