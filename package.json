{
  "name": "@tpsdev-ai/milton-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Moldable office-agent shell. One shape, any role.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "cd packages/shell && bun run build && cd ../cli && bun run build && cd ../discord && bun run build",
    "test": "cd packages/shell && bun test && cd ../cli && bun test && cd ../discord && bun test",
    "typecheck": "cd packages/shell && bun run build && bun run typecheck && cd ../cli && bun run typecheck && cd ../discord && bun run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  },
  "license": "Apache-2.0",
  "author": "tpsdev-ai",
  "repository": {
    "type": "git",
    "url": "https://github.com/tpsdev-ai/milton.git"
  }
}
